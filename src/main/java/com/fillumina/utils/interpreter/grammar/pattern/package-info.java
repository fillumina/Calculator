/**
 * This package offers pattern (REGEXP) based implementations of
 * {@link com.fillumina.utils.interpreter.GrammarElement}s.
 * It's very flexible but something like 3 times
 * slower than a more specialized approach (see the {@code fast} package).
 */
package com.fillumina.utils.interpreter.grammar.pattern;
