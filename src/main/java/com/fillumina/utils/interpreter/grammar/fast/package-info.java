/**
 * This package offers specialized implementations of
 * {@link com.fillumina.utils.interpreter.GrammarElement}s.
 * They are less flexible but some 3 times faster than the pattern based
 * implementations.
 */
package com.fillumina.utils.interpreter.grammar.fast;
