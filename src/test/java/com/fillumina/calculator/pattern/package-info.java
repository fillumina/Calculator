/**
 * This package offers pattern (REGEXP) based implementations of
 * {@link com.fillumina.utils.interpreter.GrammarElement}s.
 * They are very flexible but about 3 times
 * slower than the default specialized approach (see the {@code fast} package).
 */
package com.fillumina.calculator.pattern;
