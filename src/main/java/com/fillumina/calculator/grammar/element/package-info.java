/**
 * This package offers specialized implementations of
 * {@link com.fillumina.utils.interpreter.GrammarElement}s.
 * They are less flexible but up to 3 times faster than the pattern based
 * implementations.
 */
package com.fillumina.calculator.grammar.element;
